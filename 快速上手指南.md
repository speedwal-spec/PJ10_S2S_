# 快速上手指南 - 给同学的说明

## 🚀 5分钟开始运行

### 前置要求

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 准备数据（如果还没运行过）
python scripts/prepare_data.py
```

---

## 📋 基本使用流程

### Step 1: 训练模型

```bash
# 使用默认配置（T5-Small）
python scripts/train.py --config src/configs/ablation/baseline.yaml
```

**训练完成后会自动**:
- ✅ 保存模型到 `checkpoints_ablation/baseline/`
- ✅ 执行快速 ROUGE 评测
- ✅ 生成训练图表

### Step 2: 完整评测

```bash
# 评测刚训练的模型
python scripts/evaluate.py --ckpt checkpoints_ablation/baseline --max_samples 1000
```

**评测完成后会自动生成**:
- ✅ `results/baseline_n1000_rouge.json` - JSON 结果
- ✅ `examples/outputs.md` - Markdown 样例（符合评测要求）
- ✅ `results/baseline_n1000_report.txt` - 完整报告

---

## 🎯 尝试不同模型

### 方法1: 命令行指定（最简单）

```bash
# T5-Base (220M 参数)
python scripts/train.py \
  --config src/configs/ablation/baseline.yaml \
  --model_name google-t5/t5-base \
  --exp_id t5_base

# BART-Base (推荐，专为摘要设计)
python scripts/train.py \
  --config src/configs/ablation/baseline.yaml \
  --model_name facebook/bart-base \
  --exp_id bart_base

# PEGASUS-CNN/DM (最强，已在 CNN/DM 上预训练)
python scripts/train.py \
  --config src/configs/ablation/baseline.yaml \
  --model_name google/pegasus-cnn_dailymail \
  --exp_id pegasus_cnn_dm
```

### 方法2: 修改配置文件

编辑 `src/configs/default.yaml`:

```yaml
model:
  name: "facebook/bart-base"  # 改成你想要的模型
```

然后正常运行：
```bash
python scripts/train.py --config src/configs/ablation/baseline.yaml
```

---

## 📊 推荐的对比实验

### 实验1: 不同模型架构对比

```bash
# 1. T5-Small (基线)
python scripts/train.py --config src/configs/ablation/baseline.yaml
python scripts/evaluate.py --ckpt checkpoints_ablation/baseline --max_samples 1000

# 2. BART-Base
python scripts/train.py --config src/configs/ablation/baseline.yaml --model_name facebook/bart-base --exp_id bart_base
python scripts/evaluate.py --ckpt checkpoints_ablation/bart_base --max_samples 1000

# 3. PEGASUS-CNN/DM
python scripts/train.py --config src/configs/ablation/baseline.yaml --model_name google/pegasus-cnn_dailymail --exp_id pegasus
python scripts/evaluate.py --ckpt checkpoints_ablation/pegasus --max_samples 1000
```

**预期结果对比**:
```
模型                ROUGE-1    ROUGE-2    ROUGE-L
T5-Small           ~0.30      ~0.10      ~0.21
BART-Base          ~0.37      ~0.15      ~0.26
PEGASUS-CNN/DM     ~0.39      ~0.17      ~0.28  ← 可能最好
```

### 实验2: 消融实验（超参数影响）

```bash
# 学习率对比
python scripts/train.py --config src/configs/ablation/lr_5e_4.yaml
python scripts/evaluate.py --ckpt checkpoints_ablation/lr_5e_4

# 批次大小对比
python scripts/train.py --config src/configs/ablation/bs_8.yaml
python scripts/evaluate.py --ckpt checkpoints_ablation/bs_8

# 数据量对比
python scripts/train.py --config src/configs/ablation/data_120.yaml
python scripts/evaluate.py --ckpt checkpoints_ablation/data_120
```

---

## 💻 硬件要求

### CPU 运行（慢但可行）

```bash
# 使用小数据集快速测试
python scripts/train.py \
  --config src/configs/ablation/baseline.yaml \
  --max_train_samples 100 \
  --max_val_samples 20 \
  --epochs 2
```

**预计时间**:
- T5-Small: ~10-20 分钟（100样本，2 epochs）
- BART-Base: ~20-30 分钟

### GPU 运行（推荐）

```bash
# 正常训练
python scripts/train.py --config src/configs/ablation/baseline.yaml
```

**预计时间** (RTX 3060):
- T5-Small: ~30 分钟（2000样本，5 epochs）
- BART-Base: ~45 分钟
- PEGASUS: ~60 分钟

### 显存不足时

```bash
# 减小 batch_size，增加梯度累积
python scripts/train.py \
  --config src/configs/ablation/baseline.yaml \
  --batch_size 2 \
  --grad_accum 4
```

---

## 📁 输出文件说明

### 训练输出

```
checkpoints_ablation/{exp_id}/
├── model.safetensors        # 模型权重
├── config.json              # 模型配置
├── tokenizer.json           # 分词器
├── train_hparams.json       # 训练超参数
├── report_assets_*/         # 训练图表
│   ├── training_plot.png
│   ├── rouge_comparison.png
│   └── ...
└── visualization_*/         # 训练过程可视化
```

### 评测输出

```
results/
├── baseline_n1000_rouge.json      # JSON 结果
├── baseline_n1000_report.txt      # 文本报告
├── bart_base_n1000_rouge.json
└── bart_base_n1000_report.txt

examples/
└── outputs.md                     # Markdown 样例（符合评测要求）
```

---

## ⚠️ 常见问题

### Q1: 首次运行很慢？

**A**: 首次会下载模型和数据集，后续会使用缓存。

```bash
# 查看缓存位置
ls ~/.cache/huggingface/hub/       # 模型
ls data_cache/                      # 数据集
```

### Q2: 如何跳过下载直接使用缓存？

**A**: 确保 `data_manifest.json` 中的 `cache_dir` 指向正确的路径。

### Q3: 想重新训练同一个实验？

**A**: 删除旧的 checkpoint 或使用新的 `--exp_id`。

```bash
# 方法1: 删除旧模型
rm -rf checkpoints_ablation/baseline

# 方法2: 使用新ID
python scripts/train.py --config ... --exp_id baseline_v2
```

### Q4: 如何查看详细日志？

**A**: 训练时会实时打印进度，也可以使用 TensorBoard。

```bash
# 启动 TensorBoard
tensorboard --logdir runs/

# 浏览器访问 http://localhost:6006
```

### Q5: 网络不好，下载失败？

**A**: 已配置 HuggingFace 镜像（hf-mirror.com），应该没问题。如果还有问题：

```bash
# 手动设置镜像
export HF_ENDPOINT=https://hf-mirror.com
python scripts/train.py ...
```

---

## 📖 更多文档

- [AVAILABLE_MODELS.md](AVAILABLE_MODELS.md) - 完整模型清单
- [EVALUATE_REPORT_USAGE.md](评测报告使用说明.md) - 评测报告使用说明
- [README.md](README.md) - 项目总览
- [实验记录表.md](实验记录表.md) - 记录你的实验结果

---

## 🎓 实验建议

### 对于课程作业

**最少需要完成的实验**:
1. ✅ 基准实验（T5-Small）
2. ✅ 至少2个消融实验（如学习率、批次大小）
3. ✅ 完整评测并生成报告

**加分项**:
- ⭐ 尝试 BART 或 PEGASUS 模型
- ⭐ 对比不同模型的性能
- ⭐ 分析实验结果，得出结论

### 实验记录模板

创建 `my_experiments.md`:

```markdown
# 我的实验记录

## 实验1: 基准实验
- 模型: T5-Small
- 配置: baseline.yaml
- ROUGE-1: 0.30
- ROUGE-2: 0.10
- ROUGE-L: 0.21
- 训练时间: 30分钟

## 实验2: BART-Base
- 模型: facebook/bart-base
- 配置: baseline.yaml + --model_name
- ROUGE-1: 0.37
- ROUGE-2: 0.15
- ROUGE-L: 0.26
- 训练时间: 45分钟

## 结论
BART-Base 比 T5-Small 性能提升显著，ROUGE-1 提升 23%...
```

---

## 🆘 需要帮助？

遇到问题可以：
1. 查看相关文档
2. 检查错误信息
3. 联系项目作者

**祝实验顺利！** 🎉
