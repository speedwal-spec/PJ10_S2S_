# 实验指导：新闻标题自动生成（Seq2Seq / T5 微调）

## 一、实验在做什么？

新闻标题要在很短的长度里概括正文，是典型的**序列到序列（Seq2Seq）**问题：输入一长段**英文正文**，输出一句**短摘要/标题**（与数据集中参考句对照）。编解码器结构下，训练时常用 **Teacher Forcing**；推理时多用 **Beam Search** 等。

本实验**必做**为：在本仓库的 `train.py` 中**补全** (1) **S2S 预处理** `build_preprocess_fn` 内的 `_map_fn`；(2) 手写训练函数 **`run_s2s_training`**（**不得**用 `transformers.Trainer` 整段代劳「训练主循环」）。在此基础上完成**调参、保存权重**，使用 **`evaluate_rouge.py` 在统一协议下**得到 **ROUGE-1/2/L**，并撰写实验报告。可选再完成 **Gradio** 等。

数据默认 **`cnn_dailymail`**（亦可 **`xsum`**，见 `prepare_data.py`）。**8GB 显存**一般可跑小规模；**仅 CPU** 时务必**减小** `train.py` 顶栏中的训练/验证**子集条数**试通流程。若终端长时间无新输出，多为**从磁盘读大数据集/加载 T5/全量逐条生成**，可减小样本量或见「第七节」说明。

**你会接触的概念：** T5 的文本到文本形式、**prefix**、**`labels` 与 -100 掩码**、手写**优化/梯度累积/学习率**、**ROUGE**、Beam 与 `length_penalty` 等。


---

## 二、本资料包提供哪些内容？你要补全什么？

| 角色 | 文件/内容 | 说明 |
|--------|------------|------|
| **已提供** | `prepare_data.py` | 下载、缓存、**`data_manifest.json`**、**`sample_articles_20.json`（20 条英文样例+参考摘要，便于不手写长文就试 `predict`）** |
| **已提供** | `train.py`（**待补全骨架**） | 已含读 manifest、**`load_dataset`**、T5/Tokenizer、**`DataCollatorForSeq2Seq`（-100）**、tokenized 的 `map` 调用、**`main` 末 ROUGE 快评**与 `train_hparams.json` 写出。须你补：`_map_fn` 与 **`run_s2s_training`** 全部逻辑并去掉 `NotImplementedError` |
| **已提供** | `predict.py` | 交互/一次性推理；**已加载样例 1–20** 的快捷输入模式 |
| **已提供** | `evaluate_rouge.py` | **全课程统一**的 **ROUGE-1/2/L** 评测入口（**必按第三节「评测协议」使用**，便于互相比对） |
| **已提供** | `requirements.txt` | 依说明安装，缺包按报错补全 |
| **须自行完成** | 见上表 `train.py` 两处待补全处 | 不得用 `Trainer` 替代你对「训练主流程」的完整实现；若课程对加分/模型有另行规定，以该规定为准 |

**说明：** 若**无法联网**，可向教学方获取与 manifest 中 **cache_dir 一致**的离线数据包。**评阅用官方参考**通常不随公开资料包提供。

---

## 三、ROUGE 指标与**统一评测协议**（与成绩相关时请严格遵守）

### 3.1 三个 ROUGE 各表示什么？越高越好吗？

- **ROUGE-1（R-1）**：**词**（本脚本中 **stem 后**）的 unigram 重叠的 F 倾向；越接近参考，通常**越高越好**。  
- **ROUGE-2（R-2）**：**相邻两词** bigram 重叠；更照顾**语序与搭配**。  
- **ROUGE-L（R-L）**：基于**最长公共子序列**的 F 变体；不苛求连续大段相同。  

在**同一份参考摘要、同一生成设置、同一子集**下，**三项均为常见意义上的「越高越贴近参考答案」**（仅字面重叠，不量深层语义；换词但意思对，分也可能不高）。

### 3.2 与「标题」表述的关系

`cnn_dailymail` 的 **`highlights`** 常为**多句要点**，与「一行新闻标题」并不完全一致；本课**仍以数据内参考句为 ROUGE 金标准**，报告里可讨论「更短/更像标题」与 ROUGE 是否同向。

### 3.3 必用的评测命令与可比性

课程若将 **`evaluate_rouge.py` 输出** 作为**量化**依据，**所有同学须在报告中写明完全相同的评测命令与参数**（或**教学方**统一指定的一组），**否则 ROUGE 不可横向比较**。

- **建议统一写法示例**（具体数值以**当年课程规定**为准；若**教学方**已发布「官方一行命令」，以该发布为准）：

```bash
python evaluate_rouge.py --ckpt t5-news-checkpoint --split validation --max_samples 1000 --seed 42
```

- **子集方式**：当 `--max_samples` 大于 0 且**小于**当前划分的总条数时，脚本会对该划分**先按 `--seed`（默认 42）shuffle，再取前 N 条**，即可复现的**随机 N 条**子集，而非「固定取前 N 条顺序子集」。
- **必写清并保持一致**：`--split`（`validation` 或 `test`）、`--max_samples`（`0` 为全量，**在 CPU 上可能极慢**；默认常评**子集**以免全量在 CPU+beam 上过慢）、`--seed`（与上条 shuffle 可复现相关）、`--max_new_tokens`、`--num_beams`、`--length_penalty` 等**所有**影响抽样子集与生成的参数。  
- 换「解码/Beam/temperature**仅用于 `predict` 交互**」与「**正式填表的 ROUGE**」应**分开**说明，**勿混填**。

### 3.4 关于「成绩」的说明

- **代码能否跑通、预处理与训练步是否正确、报告中的消融与调参表**为主要评分标准。  
- 在以上基础根据 ROUGE 分数进行加分。  
- **加分/提高档**可允许更大模型或更长训，与**必做 t5-small** 分组说明，**勿混表**。

---

## 四、环境与数据

### 4.1 依赖

**Python 3.8+**，在工程目录下：

```bash
pip install -r requirements.txt
```

训练/评测常见还需：`torch`、`transformers`、`rouge-score`、`tqdm` 等，按 `requirements.txt` 与报错补全。

### 4.2 数据与样例

```bash
python prepare_data.py
```

成功后会有 **`data_manifest.json`**，且生成 **`sample_articles_20.json`（字段含 `article` 与 `reference_summary`）**，供 `predict` 用编号 **1～20** 试跑。`manifest` 中可能含 **`sample_articles_file`** 字段。

其它数据集，例如：

```bash
python prepare_data.py --dataset xsum --cache_dir ./data_cache
```

实际训练在 manifest 的 **`cache_dir`** 上，用 `datasets` 加载；**子集条数**在 `train.py` 顶栏或命令行**控制**，以便调试。

---

## 五、实现任务（建议顺序）

1. **补全** `build_preprocess_fn._map_fn`：prefix+正文、参考摘要的 token 化，组装 **`labels`**。  
2. **补全** `run_s2s_training`：`DataLoader`、`model(**batch)` 与 `loss`、**梯度累积**、优化器/（可选）调度器、验证集上 loss、`save_pretrained`。  
3. 跑通 `train.py`，确认 `t5-news-checkpoint`（或你设的 `--output_dir`）可加载。  
4. 用 **`evaluate_rouge.py`** 按**第三节**协议得到表格化 ROUGE。  
5. 用 **`predict.py`** 做**交互**试例（可选结合 `sample_articles_20` 编号输入）。  


### 5.1 说明（与第四节呼应）

- **Teacher Forcing / CE**：`DataCollator` 将 pad 的 `label` 置为 **-100**；`outputs.loss` 与之一致即可。  
- **生成**：`main` 末自带若干验证 ROUGE 快评；**正式指标以 `evaluate_rouge.py` 为准**（若与课堂规定一致）。  
- **调参**：在**固定评测命令**下尝试 `lr`、epoch、批大小/累积、训练截断、解码 `max_new_tokens` / `length_penalty` 等，并在报告中**表格式**记录。

---

## 六、提交与报告（以课程最终通知为准）

建议包含：

1. 数据与**训练/验证子集规模**、种子或是否可复现；  
2. 基座模型名（默认 **`google-t5/t5-small`**，若加分换大模型请单列）；  
3. 训练/验证 **Loss 曲线或终值** + **在统一协议下的 ROUGE-1/2/L**；  
4. 至少**两条**「正文 / 参考 / 模型输出」**对照**与简短讨论；  
5. 列出**你用于填表**的 `evaluate_rouge` **完整命令行**（与第三节一致）。

---


## 七、常见问题

**Q：下载或 HF 失败？**  
查网络/镜像，或向教学方索取离线 `cache` 与 manifest。

**Q：显存/内存不足？**  
减 `batch_size`、**训练条数**、`max_source_len`，或使用梯度累积、fp16（在实现中自行打开）。

**Q：`train` / `evaluate_rouge` 似乎卡住？**  
大缓存**首次**映射、T5 权重**首次/磁盘**加载、**全量**验证集+beam 生成在 **CPU 上**均可极慢。**先**将 `train` 子集、**`evaluate_rouge --max_samples` 小**；评测脚本在关键步骤会**有打印与 tqdm 进度**。

**Q：ROUGE 偏低？**  
先查预处理与训练步是否正确、**评测命令是否与同学一致**、参考列与 manifest 是否一致、生成是否**未**把 prefix 重算进 ROUGE 的参考端（`evaluate_rouge` 对参考用原始 summary 列）。

**Q：更像标题但 ROUGE 不涨？**  
见**第三节 3.2**；报告里可写**主观**评价与**客观**ROUGE 的关系。

---

祝实验顺利。若课程对**统一 ROUGE 命令**、**子集下界**或**给分表**有单独说明，**以该文件/课堂为准**。
