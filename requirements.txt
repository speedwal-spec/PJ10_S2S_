# ============================================
# T5 新闻摘要消融实验 - Python 依赖
# 用法: pip install -r requirements.txt
# 环境: Python 3.10+
# ============================================

# --- 数据与模型库 ---
datasets>=3.1.0
huggingface_hub>=0.26.0
transformers>=4.46.0
tokenizers>=0.20.0

# --- 深度学习框架 ---
torch>=2.0.0
accelerate>=1.0.0
safetensors>=0.7.0

# --- 分词器依赖 ---
sentencepiece>=0.2.0       # PEGASUS 模型（PegasusTokenizer 慢速版）
tiktoken>=0.7.0            # 部分分词器 fast 版转换依赖

# --- 数值与科学计算 ---
numpy>=1.24.0

# --- 评测指标 ---
rouge-score>=0.1.2
bert-score>=0.3.13         # BERTScore 语义相似度

# --- 可视化 ---
matplotlib>=3.7.0          # Loss 曲线、ROUGE 柱状图、性能雷达图
tensorboard>=2.14.0        # 训练实时监控

# --- 工具库 ---
PyYAML>=6.0               # YAML 配置加载
requests>=2.28.0           # API 调用
tqdm>=4.65.0               # 进度条

# --- Web 演示（可选） ---
gradio>=4.0.0              # A/B 对比竞技场（main.py）
