# 评测报告生成使用说明

## ✅ 功能概述

`scripts/evaluate.py` 现已支持自动生成完整的评测报告，包括：

1. **JSON 结果** - 结构化 ROUGE 分数，便于程序读取
2. **Markdown 样例** - 符合评测要求的 `examples/outputs.md`
3. **文本报告** - 详细的评测报告，包含所有信息

---

## 📋 基本用法

### 默认运行（自动生成所有报告）

```bash
python scripts/evaluate.py --ckpt checkpoints_ablation/baseline
```

**输出**:
```
============================================================
📊 ROUGE 评测结果
============================================================
  ROUGE-1: 0.2996
  ROUGE-2: 0.0950
  ROUGE-L: 0.2067
  评测样本数: 1000
============================================================

📝 正在生成评测报告...
✅ ROUGE 结果已写入: results/baseline_rouge_20260603_124043.json
✅ 生成样例已写入: examples/outputs.md (符合评测要求)
✅ 完整报告已写入: results/baseline_report_20260603_124043.txt

============================================================
📄 生成的报告文件:
============================================================
  JSON 结果:   results/baseline_rouge_20260603_124043.json
  Markdown:    examples/outputs.md (符合评测要求)
  文本报告:    results/baseline_report_20260603_124043.txt
============================================================
```

### 自定义参数

```bash
# 在测试集上评测，全量数据
python scripts/evaluate.py \
  --ckpt checkpoints_ablation/baseline \
  --split test \
  --max_samples 0

# 限制评测样本数
python scripts/evaluate.py \
  --ckpt checkpoints_ablation/lr_5e4 \
  --max_samples 100

# 同时指定自定义 JSON 输出路径（保留原有功能）
python scripts/evaluate.py \
  --ckpt checkpoints_ablation/baseline \
  --output_json my_results/custom.json
```

---

## 📁 生成的文件格式

### 1. JSON 结果 (`results/{exp_id}_rouge_{timestamp}.json`)

```json
{
  "exp_id": "baseline",
  "split": "validation",
  "n_samples": 1000,
  "rouge1": 0.2996,
  "rouge2": 0.0950,
  "rougeL": 0.2067,
  "timestamp": "2026-06-03 12:40:43"
}
```

**用途**:
- 程序化读取评测结果
- 绘制对比图表
- 实验记录管理

### 2. Markdown 样例 (`examples/outputs.md`)

```markdown
# ROUGE 评测样例 - baseline

**评测时间**: 2026-06-03 12:40:43

**ROUGE-1**: 0.2996  
**ROUGE-2**: 0.0950  
**ROUGE-L**: 0.2067  
**评测样本数**: 1000

---

## 生成样例（前 10 条）

### 样例 1

**参考摘要**:
> Jarryd Hayne quit the NRL in October to try and get into American Football.
This week, he signed a three-year contract with the San Francisco 49ers.

**模型生成**:
> Jarryd Hayne has signed a three-year contract with the 49ers.

---

### 样例 2
...
```

**用途**:
- ✅ **符合评测要求** - 直接提交作为示例输出
- 人工检查生成质量
- 论文/报告附录

### 3. 文本报告 (`results/{exp_id}_report_{timestamp}.txt`)

```
======================================================================
ROUGE 评测报告 - baseline
======================================================================

评测时间: 2026-06-03 12:40:43
Checkpoint: checkpoints_ablation/baseline
数据集划分: validation
评测样本数: 1000

----------------------------------------------------------------------
ROUGE 指标
----------------------------------------------------------------------
  ROUGE-1: 0.2996
  ROUGE-2: 0.0950
  ROUGE-L: 0.2067

----------------------------------------------------------------------
生成样例（前 5 条）
----------------------------------------------------------------------

[样例 1]
参考: Jarryd Hayne quit the NRL...
生成: Jarryd Hayne has signed...

----------------------------------------------------------------------
...
```

**用途**:
- 完整的实验记录
- 快速浏览评测结果
- 归档保存

---

## 🎯 完整评测流程

### 单实验评测

```bash
# Step 1: 训练模型
python scripts/train.py --config src/configs/ablation/baseline.yaml

# Step 2: 自动评测并生成报告
python scripts/evaluate.py --ckpt checkpoints_ablation/baseline

# Step 3: 查看生成的报告
code examples/outputs.md          # VS Code 打开
code results/baseline_report_*.txt
```

### 批量消融实验评测

```bash
# 方法1: 手动逐个评测
python scripts/evaluate.py --ckpt checkpoints_ablation/baseline
python scripts/evaluate.py --ckpt checkpoints_ablation/lr_5e4
python scripts/evaluate.py --ckpt checkpoints_ablation/bs_8_ga_1

# 方法2: 使用脚本批量评测（推荐）
for exp in baseline lr_5e4 bs_8_ga_1; do
    python scripts/evaluate.py --ckpt checkpoints_ablation/$exp --max_samples 500
done

# 方法3: 生成对比图表
python generate_comparison_plots.py
```

---

## 📊 与其他功能的配合

### 1. 训练时的快评 vs 完整评测

| 特性 | 训练快评 (`train.py`) | 完整评测 (`evaluate.py`) |
|------|---------------------|------------------------|
| 触发时机 | 训练结束后自动执行 | 手动运行 |
| 评测样本 | 默认 128 条 | 默认 1000 条（可配置） |
| 数据集 | 验证集子集 | 验证集或测试集 |
| 报告生成 | ✅ 是（新增） | ✅ 是（新增） |
| 速度 | 快（省时间） | 慢（更准确） |

**建议**:
- 训练时使用快评快速反馈
- 最终报告使用完整评测

### 2. 报告文件位置

```
PJ10_S2S_/
├── checkpoints_ablation/
│   ├── baseline/
│   │   ├── model.safetensors
│   │   └── ...
│   └── lr_5e4/
├── examples/
│   └── outputs.md              ← Markdown 样例（符合评测要求）
├── results/
│   ├── baseline_rouge_20260603_124043.json    ← JSON 结果
│   ├── baseline_report_20260603_124043.txt    ← 文本报告
│   ├── lr_5e4_rouge_20260603_124100.json
│   └── lr_5e4_report_20260603_124100.txt
└── scripts/
    ├── train.py
    └── evaluate.py
```

---

## 💡 常见问题

### Q1: 为什么没有生成报告文件？

**A**: 确保使用了正确的命令：
```bash
# ✅ 正确
python scripts/evaluate.py --ckpt checkpoints_ablation/baseline

# ❌ 错误（缺少 --ckpt）
python scripts/evaluate.py
```

### Q2: 如何更改生成的样例数量？

**A**: 修改 `src/core/report_generator.py` 中的 `max_examples` 参数：
```python
# 在 generate_all_reports() 函数中
save_examples_markdown(..., max_examples=20)  # 改为 20 条
```

### Q3: 可以在不同电脑上运行吗？

**A**: 可以！所有路径都基于项目根目录自动计算，无需修改。

### Q4: 报告文件的编码是什么？

**A**: UTF-8，兼容中文和英文。

---

## 🔧 高级用法

### 自定义报告格式

编辑 `src/core/report_generator.py`，修改以下函数：

- `save_rouge_json()` - 自定义 JSON 结构
- `save_examples_markdown()` - 自定义 Markdown 格式
- `save_full_report()` - 自定义文本报告布局

### 集成到自动化流程

```python
# run_evaluation.py
import subprocess
import sys

experiments = ["baseline", "lr_5e4", "bs_8_ga_1"]

for exp in experiments:
    ckpt = f"checkpoints_ablation/{exp}"
    cmd = [sys.executable, "scripts/evaluate.py", "--ckpt", ckpt, "--max_samples", "500"]
    subprocess.run(cmd, check=True)

print("✅ 所有实验评测完成！")
```

---

## 📚 相关文档

- [QUICK_EVAL_RESTORED.md](QUICK_EVAL_RESTORED.md) - 训练快评功能说明
- [PATH_FIX_FINAL.md](PATH_FIX_FINAL.md) - 路径管理修复
- [README.md](README.md) - 项目总览

---

**最后更新**: 2026-06-03  
**维护者**: AI Assistant
