# 项目开发记录 — T5 新闻摘要生成消融实验

> 记录项目从「实验骨架」到「完整 MLOps 体系」的开发历程与模块设计说明。
> 更新日期：2026-05-25

---

## 一、项目总览

本项目以 **T5-small** 为基础模型，针对 **CNN/DailyMail** 新闻标题自动生成任务，从一份实验骨架代码出发，逐步构建了包含 **YAML 配置管理、自动消融流水线、A/B 测试 Gradio 对比平台** 的完整实验研究体系。

### 技术栈

| 层 | 选型 |
|---|---|
| 深度学习 | PyTorch 2.0+ + Hugging Face Transformers |
| 配置管理 | PyYAML + dataclass 类型安全 |
| 推理/评测 | Beam Search + ROUGE-1/2/L (rouge-score) |
| 可视化 | Matplotlib（训练曲线、雷达图）、TensorBoard（步级监控） |
| 交互界面 | Gradio 4+（A/B 对比舱） |
| 自动化 | subprocess 流水线 + 日志 + OOM 容错 |

---

## 二、模块架构与改动记录

### 2.1 `train.py` — 核心训练脚本

**原始状态**：实验骨架，`_map_fn` 和 `run_s2s_training` 内为 `NotImplementedError`。

**完成的功能**：

```
┌─────────────────────────────────────────┐
│  main()                                  │
│  ├─ 参数解析 (argparse + --config)       │
│  ├─ 数据加载 (manifest → Dataset)        │
│  ├─ Tokenize (build_preprocess_fn)        │
│  ├─ DataCollatorForSeq2Seq (-100 masked) │
│  ├─ run_s2s_training()                   │
│  ├─ save_hparams_json()                  │
│  ├─ _quick_rouge_eval()                  │
│  └─ _generate_report_visuals()           │
│      ├─ training_curve.png               │
│      ├─ rouge_comparison.png             │
│      ├─ performance_radar.png            │
│      └─ loss_evolution.png               │
└─────────────────────────────────────────┘
```

**关键改动**：

| 函数/模块 | 说明 |
|---|---|
| `build_preprocess_fn._map_fn` | prefix+正文 token 化 + `text_target` API 编码标签 + `-100` padding |
| `run_s2s_training` | 手写训练循环，含 DataLoader、AdamW、线性调度器、warmup、AMP、梯度累积、Early Stopping |
| `save_hparams_json` | 将训练超参写入 `train_hparams.json`，供 ModelManager/Gradio 读取 |
| `main` 中 `--config` 分支 | YAML 配置合并：`load_config` / `load_full_config` + `merge_with_cli` |
| 命令行参数 | 扩展至 20+ 参数：lr、batch_size、grad_accum、epochs、patience、seed、num_beams、length_penalty、weight_decay、max_grad_norm 等 |

**Early Stopping 逻辑**：
- 每个 epoch 后比较验证 loss
- 若创新低则保存 `save_pretrained`，重置计数器
- 连续 `patience` 次未降低则提前终止训练

---

### 2.2 `configs/` — YAML 配置管理系统（全新）

**设计目的**：将硬编码超参数外部化，支持继承链、多环境配置。

| 文件 | 用途 |
|---|---|
| `config_manager.py` | YAML 加载器 + 继承解析 + dataclass 定义 + CLI 合并 |
| `default.yaml` | 全局默认超参（所有实验的基类） |
| `paths.yaml` | 路径配置（不同电脑可修改） |
| `hardware.yaml` | 硬件配置（GPU/CPU/AMP 开关） |
| `ablation/baseline.yaml` | 消融基准（继承 default.yaml，覆盖 `max_train_samples: 80`） |
| `ablation/lr_0_001.yaml` | 高学习率实验（继承 baseline.yaml） |
| `ablation/bs_8.yaml` | 大 Batch Size 实验（继承 baseline.yaml） |
| `ablation/samples_40.yaml` | 少量数据实验（继承 baseline.yaml） |

**配置继承链**：
```
default.yaml          ← 最底层（所有公共默认值）
  └─ baseline.yaml    ← 消融基准（max_train_samples=80, max_val_samples=20）
       ├─ lr_0_001.yaml     （lr=0.001）
       ├─ bs_8.yaml         （batch_size=8, grad_accum=1）
       └─ samples_40.yaml   （max_train_samples=40, max_val_samples=10）
```

**类型安全的 dataclass 体系**：

```
ExperimentConfig
├─ DataConfig           (manifest, max_train_samples, max_source_length, prefix...)
├─ TrainingConfig       (epochs, batch_size, grad_accum, lr, warmup_ratio, weight_decay...)
├─ EarlyStoppingConfig  (patience, monitor)
├─ InferenceConfig      (max_new_tokens, num_beams, length_penalty, early_stopping...)
├─ EvaluationConfig     (split, max_samples, batch_size)
├─ VisualizationConfig  (output_dir, font, dpi)
├─ PathConfig           (output_dir, ablation_base, cache_dir, samples_file...)
├─ EnvironmentConfig    (hf_endpoint, device, amp, cpu_threads...)
└─ LoggingConfig        (tensorboard_dir)
```

---

### 2.3 `core/visualization.py` — 训练内联可视化（全新）

| 函数 | 输出 |
|---|---|
| `save_training_plot` | 训练/验证 Loss 曲线 + 超参标注 |
| `plot_rouge_comparison` | ROUGE-1/2/L 柱状图 |
| `plot_performance_radar` | 多维度性能雷达图 |
| `plot_loss_rouge_evolution` | 简洁版 Loss 演化曲线 |
| `try_compute_rouge` | 内联 ROUGE 计算（依赖 rouge-score） |

调用位置：`_generate_report_visuals` 在 `train.py` 训练结束时自动调用，输出到 `report_assets_{timestamp}/` 目录。

---

### 2.4 `core/model_manager.py` — 双槽位模型管理器（全新）

**职责**：
1. 扫描硬盘上的模型资产（消融目录 + 传统目录）
2. 管理 A/B 双槽位模型热切换
3. 防止双倍显存峰值 OOM
4. 向后兼容旧版 checkpoint 的 hparams 推断

**3 级超参补全策略**（解决旧 checkpoint 缺失字段问题）：

```
1. train_hparams.json  ← 新版本训练输出的完整 JSON
2. 目录名推断           ← 兼容旧命名：lr_1e3, bs2_ga4, data_120, len_long
3. default.yaml 兜底    ← configs/default.yaml 的统一默认值
```

**目录名推断支持的模式**：
- `lr_1e3` → lr=0.001, `lr_1e4` → lr=0.0001
- `bs2` / `bs_8` → batch_size
- `ga4` / `ga_1` → grad_accum, `no_accum` → grad_accum=1
- `data_120` → max_train_samples=120
- `len_long` → max_target_len=80, `len_short` → max_target_len=20

**显存安全的热切换机制**：
```
load_model(exp_id, slot="a"):
  1. 释放旧模型引用 → None
  2. gc.collect() + torch.cuda.empty_cache()
  3. 加载新模型 (tie_weights → to(device))
  4. 绑定到卡槽
```

---

### 2.5 `ui/gradio_app.py` — A/B 对比界面（全新）

**功能布局**：

```
┌─────────────────────────────────────────────┐
│              Neural News Summarizer          │
├──────────────┬──────────────────────────────┤
│ 共享数据流    │      A/B 对比舱               │
│              │  ┌─────────┬──────────────┐   │
│  原始正文     │  │  模型 A  │  模型 B      │   │
│  (Textbox)   │  │  Dropdown│  Dropdown    │   │
│              │  │  状态栏  │  状态栏       │   │
│  清除 / 提交  │  │  属性卡  │  属性卡       │   │
│              │  │  输出框  │  输出框       │   │
│  生成参数     │  └─────────┴──────────────┘   │
│  (Slider)    │                               │
├──────────────┴──────────────────────────────┤
│           快捷测试数据 (动态抓取样例)          │
└─────────────────────────────────────────────┘
```

**主题配色（深紫蓝暗色方案）**：

| 元素 | 色值 |
|---|---|
| 页面背景 | `#1e1e3a` |
| 容器底色 | `#161630` |
| 输入框底色 | `#252548` |
| 代码文字 | `#f0d060`（暖金色） |
| 普通文字 | `#e8e8e8` |
| 滑动条 | `#7c6ff0`（紫色） |

**CSS 修复项**：
- Markdown 行内代码（`` `<code>` ``）文字颜色（白底+白字→#f0d060 暖金色）
- 快捷测试样例文字（黑字→#d0d0e0 柔白紫）
- Gradio 4+ `<button>` 渲染兼容
- 表格浅色背景（#2d2d52 + 8px 圆角）
- readonly 文本框颜色

**动态测试样例**：
- 从 `sample_articles_20.json` 加载前 5 条
- 文件不存在时使用硬编码回退样例
- 点击自动填入输入框

---

### 2.6 `run_pipeline.py` — 自动化消融流水线（全新）

**执行流程**：
```
1. 加载 full_config（读取 paths + evaluation）
2. 扫描 configs/ablation/*.yaml（跳过 baseline.yaml）
3. 对每个配置：
   a. python train.py --config configs/ablation/{file} --output_dir {ablation_base}
   b. python evaluate_rouge.py --ckpt {ablation_base}/{exp_id} --output_json results_{exp_id}.json
4. OOM 容错：子进程失败 → catch → 跳过 → 继续下一组
5. 全程日志记录到 pipeline_{timestamp}.log
```

**关键修复**：之前训练用 `t5-news-checkpoint/{exp_id}`，评估用 `checkpoints_ablation/{exp_id}`，路径不一致导致评估失败。已添加 `--output_dir {OUTPUT_BASE}` 统一输出目录。

---

### 2.7 `evaluate_rouge.py` — 统一评测入口

**改动**：
- 新增 `--config` 参数，支持从 YAML 加载评测默认值
- 新增 `--seed` 参数（默认 42），子集抽样可复现
- 评测结果写入结构化 JSON（含生成参数、抽样方式、模型路径）
- 支持 `--split test` 和 `--split validation`

---

### 2.8 `predict.py` — 交互推理

**改动**：
- 新增 `--config` 参数，支持从 YAML 加载推理默认值
- 保持交互模式（1-20 编号选样例、t 多行输入、q 退出）

---

## 三、文件结构（最终状态）

```
PJ10_S2S_/
├── train.py                        # 核心训练脚本（已补全）
├── evaluate_rouge.py               # 统一 ROUGE 评测
├── predict.py                      # 交互推理
├── prepare_data.py                 # 数据准备
├── run_pipeline.py                 # ★ 自动化消融流水线
├── requirements.txt                # Python 依赖
├── data_manifest.json              # 数据集元数据
├── sample_articles_20.json         # 20 条测试样例
├── .gitignore
│
├── configs/                        # ★ YAML 配置系统
│   ├── config_manager.py           #   配置加载 & 继承解析
│   ├── default.yaml                #   默认超参
│   ├── paths.yaml                  #   路径配置
│   ├── hardware.yaml               #   硬件配置
│   └── ablation/                   #   消融实验配置
│       ├── baseline.yaml
│       ├── lr_0_001.yaml
│       ├── bs_8.yaml
│       └── samples_40.yaml
│
├── core/                           # ★ 核心模块
│   ├── __init__.py
│   ├── model_manager.py            #   双槽位模型管理器
│   └── visualization.py            #   训练内联可视化
│
├── ui/                             # ★ 交互界面
│   ├── __init__.py
│   └── gradio_app.py               #   A/B 对比 Gradio 界面
│
├── t5-news-checkpoint/             # 单次训练输出（.gitignore）
├── checkpoints_ablation/           # 消融实验输出（.gitignore）
├── data_cache/                     # 数据集缓存（.gitignore）
├── runs/                           # TensorBoard 日志（.gitignore）
│
└── 实验指导.md                     # 实验要求文档
```

## 四、关键问题与解决方案

| 问题 | 原因 | 解决方案 |
|---|---|---|
| Gradio 显示 N/A | 旧 checkpoint 无 lr/batch_size 字段 | 3 级 hparams 补全（JSON→目录名→默认值） |
| `_cfg.project.seed` 报错 | `ExperimentConfig.seed` 是直接属性 | 改为 `_cfg.seed` |
| 流水线评估失败 | train 和 eval 指向不同目录 | `run_pipeline.py` 加 `--output_dir` |
| 白底框字不可见 | `<code>` 默认白底+白字 | CSS 注入暖金色 `#f0d060` |
| 快捷测试字黑色 | Gradio 4+ 渲染 `<button>` 而非 `<td>` | 增加 button 层级 CSS 选择器 |
| 梯度累积除数错误 | `loss/step` 而非 `loss/(有效累积步)` | 修正为 `loss/actual_accum_steps` |

---

## 五、实验指导.md 对照

| 要求 | 状态 | 实现位置 |
|---|---|---|
| `_map_fn` — prefix+正文 token 化 + labels | ✅ | `train.py` L89-112 |
| 手写训练循环（无 Trainer） | ✅ | `train.py` L117-272 |
| `model(**batch).loss` + DataCollator -100 | ✅ | `train.py` L196-205 |
| `save_pretrained` 保存 | ✅ | `train.py` L258-259 |
| `train_hparams.json` 写出 | ✅ | `train.py` L274-296 |
| `evaluate_rouge.py` 统一评测 | ✅ | `evaluate_rouge.py` |
| `predict.py` 交互推理 | ✅ | `predict.py` |
| ROUGE-1/2/L 三项输出 | ✅ | `evaluate_rouge.py` L88-110 |
| `--seed` shuffle 可复现子集 | ✅ | `evaluate_rouge.py` L207-208 |

---

*本文档记录项目从实验骨架到完整体系的全部改动，供后续开发和维护参考。*
