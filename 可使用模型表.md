# 可用模型清单与对比实验指南

## 🎯 核心原理

本项目基于 **Hugging Face Transformers**，支持所有 Seq2Seq（序列到序列）架构的预训练模型。

**只需修改配置中的 `model.name`，无需修改任何代码！**

---

## 📚 推荐模型清单

### 1. T5 系列（Text-to-Text Transfer Transformer）

| 模型名称 | 参数量 | 显存需求 | 特点 | 推荐场景 |
|---------|--------|---------|------|---------|
| `google-t5/t5-small` | 60M | 4GB | 快速训练，基线模型 | ✅ 当前默认，适合调试 |
| `google-t5/t5-base` | 220M | 8GB | 平衡性能和速度 | ⭐ 推荐用于正式实验 |
| `google-t5/t5-large` | 770M | 16GB | 性能优秀 | 需要好显卡 |
| `google-t5/t5-3b` | 3B | 32GB+ | 超强性能 | 不推荐（太慢） |

**优势**:
- ✅ 统一的 Text-to-Text 范式
- ✅ 训练稳定，收敛快
- ✅ 社区支持好

**劣势**:
- ❌ 不是专为摘要设计

---

### 2. BART 系列（Bidirectional and Auto-Regressive Transformers）

| 模型名称 | 参数量 | 显存需求 | 特点 | 推荐场景 |
|---------|--------|---------|------|---------|
| `facebook/bart-base` | 140M | 8GB | 专为摘要和翻译设计 | ⭐⭐ 强烈推荐 |
| `facebook/bart-large` | 400M | 16GB | SOTA 级别性能 | 需要好显卡 |
| `facebook/bart-large-cnn` | 400M | 16GB | 已在 CNN/DM 上微调 | ⭐⭐⭐ 最佳选择 |

**优势**:
- ✅ **专为文本摘要设计**
- ✅ 在 CNN/DailyMail 上有预训练版本
- ✅ 通常比 T5 效果好 2-5%

**劣势**:
- ❌ 训练稍慢于 T5

---

### 3. PEGASUS 系列（Pre-training with Extracted Gap-sentences for Abstractive SUmmarization）

| 模型名称 | 参数量 | 显存需求 | 特点 | 推荐场景 |
|---------|--------|---------|------|---------|
| `google/pegasus-xsum` | 568M | 12GB | 在 XSum 数据集上预训练 | 新闻摘要 |
| `google/pegasus-cnn_dailymail` | 568M | 12GB | **已在 CNN/DM 上预训练** | ⭐⭐⭐ 强烈推荐 |
| `google/pegasus-large` | 568M | 12GB | 通用摘要 | 多领域 |

**优势**:
- ✅ **专为 abstractive summarization 设计**
- ✅ 在 CNN/DailyMail 上有专门的预训练版本
- ✅ 通常是目前最好的摘要模型

**劣势**:
- ❌ 训练较慢
- ❌ 需要更多显存

---

### 4. LED 系列（Longformer-Encoder-Decoder）

| 模型名称 | 参数量 | 显存需求 | 特点 | 推荐场景 |
|---------|--------|---------|------|---------|
| `allenai/led-base-16384` | 160M | 8GB | 支持超长文档（16K tokens） | 长文章摘要 |

**优势**:
- ✅ 支持超长输入
- ✅ 基于 Longformer 架构

**劣势**:
- ❌ 新模型，稳定性一般
- ❌ 对于短文档优势不明显

---

### 5. 其他值得尝试的模型

| 模型名称 | 参数量 | 显存需求 | 特点 |
|---------|--------|---------|------|
| `sshleifer/distilbart-cnn-12-6` | 80M | 4GB | BART 的蒸馏版本，快速 |
| `tuner007/pegasus_paraphrase` | 568M | 12GB | PEGASUS 变体 |
| `google/byt5-small` | 未知 | 4GB | 基于字节的 T5，多语言 |

---

## 🔧 使用方法

### 方法1：修改配置文件（推荐）

编辑 `src/configs/default.yaml`：

```yaml
# ─── 模型 ───
model:
  # 从以下选择一个：
  name: "google-t5/t5-small"              # 当前默认
  # name: "google-t5/t5-base"             # T5-Base
  # name: "facebook/bart-base"            # BART-Base
  # name: "facebook/bart-large-cnn"       # BART-Large-CNN (推荐)
  # name: "google/pegasus-cnn_dailymail"  # PEGASUS-CNN/DM (推荐)
```

然后运行：
```bash
python scripts/train.py --config src/configs/ablation/baseline.yaml
```

### 方法2：命令行覆盖

```bash
# 使用 BART-Base
python scripts/train.py \
  --config src/configs/ablation/baseline.yaml \
  --model_name facebook/bart-base

# 使用 PEGASUS
python scripts/train.py \
  --config src/configs/ablation/baseline.yaml \
  --model_name google/pegasus-cnn_dailymail
```

### 方法3：创建新的消融实验配置

创建 `src/configs/ablation/model_bart.yaml`：

```yaml
extends: ../default.yaml

experiment:
  id: "bart_base"
  description: "使用 BART-Base 模型"

model:
  name: "facebook/bart-base"
```

然后运行：
```bash
python scripts/train.py --config src/configs/ablation/model_bart.yaml
```

---

## 📊 对比实验建议

### 实验设计

#### 实验1：不同模型架构对比
```bash
# T5-Small (基线)
python scripts/train.py --config src/configs/ablation/baseline.yaml
python scripts/evaluate.py --ckpt checkpoints_ablation/baseline

# T5-Base
python scripts/train.py --config src/configs/ablation/baseline.yaml --model_name google-t5/t5-base --exp_id t5_base
python scripts/evaluate.py --ckpt checkpoints_ablation/t5_base

# BART-Base
python scripts/train.py --config src/configs/ablation/baseline.yaml --model_name facebook/bart-base --exp_id bart_base
python scripts/evaluate.py --ckpt checkpoints_ablation/bart_base

# PEGASUS-CNN/DM
python scripts/train.py --config src/configs/ablation/baseline.yaml --model_name google/pegasus-cnn_dailymail --exp_id pegasus_cnn_dm
python scripts/evaluate.py --ckpt checkpoints_ablation/pegasus_cnn_dm
```

**预期结果**:
```
模型                ROUGE-1    ROUGE-2    ROUGE-L
T5-Small           0.30       0.10       0.21
T5-Base            0.35       0.14       0.25
BART-Base          0.37       0.15       0.26
PEGASUS-CNN/DM     0.39       0.17       0.28  ← 可能最好
```

#### 实验2：相同架构不同规模
```bash
# T5-Small vs T5-Base vs T5-Large
python scripts/train.py --config ... --model_name google-t5/t5-small --exp_id t5_small
python scripts/train.py --config ... --model_name google-t5/t5-base --exp_id t5_base
python scripts/train.py --config ... --model_name google-t5/t5-large --exp_id t5_large
```

**分析点**:
- 参数量增加带来的性能提升
- 训练时间的增长曲线
- 性价比分析

---

## ⚠️ 注意事项

### 1. 首次使用会下载模型

```
Downloading model: facebook/bart-base...
Model saved to: ~/.cache/huggingface/hub/
```

**解决方法**: 提前下载或使用镜像
```bash
# 设置 HuggingFace 镜像（已在 paths.yaml 中配置）
export HF_ENDPOINT=https://hf-mirror.com
```

### 2. 显存不足

**错误**: `CUDA out of memory`

**解决方法**:
```bash
# 减小 batch_size
python scripts/train.py --batch_size 2 --grad_accum 4

# 或使用更小的模型
python scripts/train.py --model_name google-t5/t5-small
```

### 3. 训练时间差异

| 模型 | 相对训练时间 (epochs=5, samples=2000) |
|------|-------------------------------------|
| T5-Small | 1x (约 30 分钟) |
| T5-Base | 2x (约 1 小时) |
| BART-Base | 2.5x (约 1.5 小时) |
| PEGASUS | 3x (约 2 小时) |
| T5-Large | 5x (约 2.5 小时) |

---

## 💡 实验结论模板

完成对比实验后，可以这样写分析报告：

### 模型架构对比分析

**实验设置**:
- 数据集: CNN/DailyMail (train=2000, val=400)
- 训练轮数: 5 epochs
- 超参数: lr=0.0003, batch_size=4, grad_accum=2

**结果对比**:

| 模型 | ROUGE-1 | ROUGE-2 | ROUGE-L | 训练时间 | 参数量 |
|------|---------|---------|---------|---------|--------|
| T5-Small | 0.30 | 0.10 | 0.21 | 30min | 60M |
| BART-Base | 0.37 | 0.15 | 0.26 | 45min | 140M |
| PEGASUS-CNN/DM | 0.39 | 0.17 | 0.28 | 60min | 568M |

**关键发现**:

1. **PEGASUS 表现最佳**: 由于在 CNN/DM 上预训练，ROUGE-1 比 T5-Small 提升 30%
2. **BART 优于 T5**: 专为摘要设计，在相同参数量下性能更好
3. **性价比分析**: BART-Base 是性能和速度的最佳平衡点

**结论**:
- 对于资源有限的场景，推荐使用 **BART-Base**
- 追求最佳性能时，选择 **PEGASUS-CNN/DM**
- T5-Small 适合快速原型验证

---

## 📖 相关资源

- [Hugging Face Model Hub](https://huggingface.co/models?pipeline_tag=summarization)
- [T5 论文](https://arxiv.org/abs/1910.10683)
- [BART 论文](https://arxiv.org/abs/1910.13461)
- [PEGASUS 论文](https://arxiv.org/abs/1912.08777)

---

**最后更新**: 2026-06-03  
**维护者**: AI Assistant
