# configs/ablation/len_256.yaml
# 消融实验 - 短序列
extends: baseline.yaml

experiment:
  id: "len_256"
  description: "输入长度=256, 生成长度=30（对比基准512/40）"

data:
  max_source_length: 256
  max_target_length: 30
