# configs/ablation/data_120.yaml
# 消融实验 - 中等数据量（对应实验记录表 Exp-5c）
extends: baseline.yaml

experiment:
  id: "data_200"
  description: "200条训练数据（对比基准400条）"

data:
  max_train_samples: 200
  max_val_samples: 50
